<!--
 * Course: MAD204-01
 * Assignment: Assignment 3
 * Student: Darshilkumar Karkar (A00203357)
 * Date: 2025-12-12
 * Description: Layout for a single item (a card) in the notes RecyclerView.
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/card_background_light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="686dp"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- TextView for the note's title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="327dp"
            android:layout_height="74dp"
            android:layout_marginBottom="4dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/md_theme_onSurface"
            android:textSize="34sp"
            android:textStyle="bold"
            tools:text="Example Note Title" />

        <!-- TextView for the note's content -->
        <TextView
            android:id="@+id/tvContent"
            android:layout_width="339dp"
            android:layout_height="140dp"
            android:ellipsize="end"
            android:maxLines="3"
            android:textColor="@color/md_theme_onSurfaceVariant"
            android:textSize="20sp"
            tools:text="This is a preview of the note content. It can span multiple lines but will be truncated if it is too long." />

    </LinearLayout>

</androidx.cardview.widget.CardView>
